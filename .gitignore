.env
.env.local
*.env
logs/
tmp/
build/
dist/
models/
*.gguf
*.bin
*.so
*.o
__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/settings.json
.cursor/settings.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# v0.3.0 — plugin install owns the MCP wiring; project-scoped .mcp.json
# at repo root would create a duplicate 'qwen-agent-server' registration
# alongside the plugin's 'supervisor' entry. Don't bring it back.
.mcp.json

# Python virtualenvs — never committed (recreate from requirements-dev.txt)
.venv/
venv/

# Local exploration / scratch — never committed
notes/
