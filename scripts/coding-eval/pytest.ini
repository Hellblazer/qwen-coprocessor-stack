[pytest]
# Offline by default: the live arm-driver rail tests require claude/qwen-code/
# qwentescence/the spawned supervisor and take minutes-to-tens-of-minutes.
# Run them explicitly with `pytest -m integration`.
markers =
    integration: requires live backends (claude / qwen-code / qwentescence / supervisor); slow
addopts = -m "not integration"
